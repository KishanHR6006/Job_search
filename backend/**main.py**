"""
Main entry point for FastAPI deployment
"""

from api_server import app

# This allows Vercel/other platforms to find the app
if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=8000)
